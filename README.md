# block_sparse_attn_wheels

Builds release wheels for:

- Block-Sparse Attention
- SageAttention
- FlashAttention

All wheels are built in the same CUDA container against the same Python and PyTorch versions from `pyproject.toml`.
